NHIỆM VỤ :
- Tìm hiểu về pyspark
- Chuyển các phần EDA, model sang bằng pyspark.
- Tìm hiểu về api, đưa model lên api.
  + Chọn 1 bài toán có dữ liệu id user, sau đó dùng api để triển khai model.
    Api nhập được id user, sau đó hiện ra kết quả của model.
  + Load test api và stress test api.
  + Biết được cache.

________________________________________________________________________________
CHI TIẾT CÔNG VIỆC DỰ KIẾN:
- PySpark DataFrame :
  + đọc CSV/parquet: spark.read.csv, schema
  + Thao tác DataFrame: select, filter, withColumn, groupBy, agg
  + xử lý null, duplicate, kiểu dữ liệu
  + window function
  + cache, persist, unpersist
  + ghi output ra parquet/csv
Mục tiêu: chuyển được các bước EDA hiện tại từ pandas sang PySpark, 
          đặc biệt các bảng tổng hợp như revenue theo ngày, customer spend, category revenue, return rate.

-  Chuyển EDA chart sang Spark theo từng chart:
  + Chọn 3-5 chart quan trọng nhất:
    revenue/profit theo thời gian
    doanh thu theo category
    AOV theo customer segment
    return rate theo category
    discount rate vs gross margin
  Câu hỏi business
    -> bảng cần dùng
    -> grain sau khi aggregate
    -> code PySpark tạo bảng
    -> toPandas() để vẽ chart
__________________________________________________________________________________
Học cách sử dụng spark với dữ liệu
Data lấy từ Google Analytics Customer Revenue Prediction
(https://www.kaggle.com/competitions/ga-customer-revenue-prediction/overview)
Gồm
- train_v2.csv
- test_v2.csv
Các công việc:
- Đọc dữ liệu bằng Spark và chuyển sang Parquet
- Chọn bài toán
- EDA bằng spark.
- Modeling
- API
Chi tiết thực hiện các nhiệm vụ dưới đây.
________________________________________________________________________________
Bước đầu: Đọc dữ liệu
Mục tiêu: Tạo dược file Parquet sạch hơn từ các file train_v2, test_v2
thành -> data_pyspark_parquet/train_sessions/ và data_pyspark_parquet/test_sessions/
- Bước 1: Khởi tạo SparkSession.
- Bước 2: Đọc thử header và schema CSV
- Bước 3: Đọc sample nhỏ
- Bước 4: Chọn cột cần giữ ở mức session-level
- Bước 5: Parse các cột json quan trọng
- Bước 6: Tạo các cột numeric sạch
- Bước 7: Tạo label mua hàng -> phục vụ bài phân loại sau này.
- Bước 8: Parse thêm device, geoNetwork, trafficSource hay là các cột quan trọng
- Bước 9: Drop các cột json gốc nặng
- Bước 10: Thử sample ra parquet.
- Bước 11: Ghi ra full train ra parquet.
- Bước 12: Làm tương tự với test.
- Bước 13: Kiểm tra lại sau khi ghi parquet.
- Bước 14: Ghi lại kết quả log.